# MindMate AI — Core Requirements (used on Render)
# Heavy ML packages (transformers, torch) are OPTIONAL.
# Without them the app uses fast rule-based emotion detection + Groq API.
# This keeps the Render free-tier build under ~500 MB.

# LLM & LangChain (lightweight)
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-ollama>=0.1.0

# Groq API (free, fast — primary AI provider)
groq>=0.9.0

# API Server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
numpy>=1.26.0

# ── Optional ML (uncomment locally or on paid Render plan) ──────────────────
# Uncomment these for HuggingFace emotion model support (needs ~3 GB RAM):
# transformers>=4.40.0
# sentence-transformers>=3.0.0
# torch>=2.2.0
# torchvision>=0.17.0
# huggingface_hub>=0.23.0
# chromadb>=0.5.0
# faiss-cpu>=1.8.0
# langchain-chroma>=0.1.0
